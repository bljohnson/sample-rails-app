<h1>New Article</h1>

<!-- :article tells form_for helper what form is for
FormBuilder object - represented by f - used to build two labels and two text fields
a call to submit on the f object creates a submit button for form -->
<%= form_for :article, url: articles_path do |f| %>

<!-- check for errors -->
<% if @article.errors.any? %>
<div id="error_explanation">
	<h2>
		<!-- pluralize is rails helper that takes a number and a string. if number is >1, string automatically pluralized -->
		<%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:
	</h2>
	<!-- show list of all errors -->
	<ul>
		<% @article.errors.full_messages.each do |msg| %>
		<li><%= msg %></li>
		<% end %>
	</ul>
</div>
<% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<!-- link to go back to the index action -->
<%= link_to 'Back', articles_path %>
